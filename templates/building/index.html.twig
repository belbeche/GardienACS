{% extends 'base.html.twig' %}

{% block title %}Hello BuildingController!{% endblock %}

{% block body %}
<div class="container">

    <h1>formulaire d'ajout</h1>

    {{ form_start(form) }}
    {{ form_row(form.Floor) }}
    {{ form_row(form.Lights, {attr: {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.Users) }}
    {{ form_end(form) }}
</div>


{% endblock %}
